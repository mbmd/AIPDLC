<!-- Copyright (c) 2026 Mohammad Maheri. Licensed under Apache 2.0. See LICENSE. Attribution required - see NOTICE. -->
<!-- TEMPLATE — emitted to .specify/memory/constitution.md when buildProfile: spec-driven-speckit. Placeholders filled by constitution-mapping.md. Zero project-specific content here (AI-DWG Rule 5). -->
---
generatedBy: AI-DWG
generatedVersion: {version}
source: {rules-cluster-path}
generatedOn: {ISO-date}
ownership: hybrid
---

# {project-name} — Project Constitution

> This constitution states the governing **principles** the SpecKit workflow reads before planning and implementing. Each principle **references** the canonical rules that carry its enforceable detail — it does not duplicate them. When a principle and a referenced rule differ, the rule in `{rules-cluster-path}` is authoritative.

## How to use this constitution

Read these principles before `/plan` and before `/implement`. They set the non-negotiable intent for every change. For the exact, checkable form of any constraint, follow the `→ see` pointer to the canonical rule file — that is where the MUST/NEVER wording lives.

## Principles

<!-- One principle per justified rule category, numbered per constitution-mapping.md ordering. Omit any category absent from rules/. Renumber contiguously — no gaps. -->

### Principle {n}: {principle-title}

{principle-intent-statement — one to few sentences on why this constraint exists and what it governs.}

→ see `{rules-file-path}`

<!-- repeat the Principle block per justified category -->

## Amendment

This constitution is `hybrid`-owned: AI-DWG seeds it, the team may hand-edit principle statements, and the SpecKit workflow may amend it. Team edits are preserved on reconciliation. When a whole rule category is added or removed in `{rules-cluster-path}`, AI-DWG refreshes the principle set on the next generation; edits within an existing category need no constitution change, because principles reference categories rather than copy rules.

---

*Generated by AI-DWG for the SpecKit build method. Principles reference the canonical rules; they never duplicate them.*
