<!-- Copyright (c) 2026 Mohammad Maheri. Licensed under Apache 2.0. See LICENSE. Attribution required - see NOTICE. -->
# `aidlc/` Tree — Output Templates

> Output templates for the files the `aidlc/` emitter writes. 100% generic — `{placeholder}` for every project-specific value. Authority for structure: `common/aidlc-v2-output-contract.md`.

---

## Template: `memory/team.md`

```markdown
---
status: active
pairing: feedforward-only
---
# Team Practices — {project_name}

## Way of Working
{branching model, review cadence, pair/mob stance — from AI-ADLC git-workflow + AI-DWG team-agreements + AI-POLC definition of done}

## Code Style
{naming + language conventions — from AI-ADLC tech stack + naming-conventions}

## Testing Posture
{test types, coverage stance — from AI-ADLC quality attributes, or AI-TGE ## Testing Posture when active}

## Deployment
{deploy strategy, rollback, environment promotion — from AI-ADLC infra + AI-DWG CI/CD mapping}
```

---

## Template: `memory/project.md`

```markdown
---
status: active
pairing: {feedforward-only | <sensor-id> where a sensor covers these rules}
---
# Project Rules — {project_name}

## Security
{MUST/NEVER security constraints — from AI-ADLC security & identity architecture}

## Architecture
{architecture rules + module boundaries — from AI-ADLC architecture principles + module structure}

## Accessibility
{a11y target — from AI-UXD accessibility baseline}   <!-- omit if no AI-UXD -->

## Domain Language
{ubiquitous-language terms — from AI-ADLC domain context}
```

---

## Template: `memory/phases/{phase}.md`

```markdown
---
status: active
pairing: feedforward-only
---
# {Phase} Phase Rules

{PG-* gate rules routed to this phase per phase-gates-generator.md. Group and label by originating AI-GCE phase where a file receives from more than one (construction.md).}
```

### `ideation.md` — the non-blank empty case

```markdown
---
status: active
pairing: feedforward-only
---
# Ideation Phase Rules

_No AI-GCE phase gates apply at ideation — this phase precedes governance (idea shaping, upstream of the gated lifecycle). This file is intentionally rule-free, not incomplete._
```

---

## Template: `knowledge/<agent>/{topic}.md`

```markdown
---
generatedBy: AI-DWG
generatedVersion: {version}
source: {upstream document path}
generatedOn: {ISO-8601 timestamp}
ownership: generated
---
# {Topic} — Reference

{reference prose / tables / diagrams — the knowledge portion of the source steering, routed per knowledge-routing.md}
```

---

## Template: `.governance/AIDLC_SENSOR_WIRING.md`

```markdown
# AI-DLC Sensor Wiring Instructions

Generated by AI-DWG. These manifests exist but do NOT fire until each is imported by a stage.
The preferred path is that AI-DLC v2's own learning loop applies this wiring; otherwise apply it by hand.
AI-GCE verifies the wiring landed.

| Sensor (`id`) | Import into stage | Line to add |
|---|---|---|
| `{sensor-id}` | `{stage-slug}` | `{exact import line}` |
```

---

*Developer-side templates · AI-DWG `aidlc/` tree · © Mohammad Maheri*
