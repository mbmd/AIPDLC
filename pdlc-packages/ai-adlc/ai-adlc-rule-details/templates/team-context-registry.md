<!-- Copyright (c) 2026 Mohammad Maheri. Licensed under Apache 2.0. See LICENSE. Attribution required - see NOTICE. -->
# Team–Context Registry

**Document Status:** {Draft / Review / Approved}
**Version:** {n.n}
**Date:** {YYYY-MM-DD}
**Author:** {Role}
**Extension:** team-topologies (opt-in) · **Produced at:** Stage 5 (Container Design)

> Produced only when the `team-topologies` extension is active. This is the **authoritative** Team ↔ Bounded-Context ↔ Service ownership map and the source of the `TEAM-*` / `BC-*` / `SVC-*` identity. It is **machine-readable** — AI-POLC tags backlog with it, AI-UXD tags UX with it, and AI-DWG reads it to partition the workspace (team-granular, never per-service). Pairs with `team-topology-map.md`.

---

## 1. Correlation Key

| Key | Value |
|-----|-------|
| Project ID | {PRJ-{ABBREV}-{YYYY}-{NNN}} |
| Registry Version | {n.n} |

> The `TEAM-*` / `BC-*` / `SVC-*` identities below are **finer-grained correlation keys** that ride alongside the project-wide `projectId`. They are registered per `contracts/TRACEABILITY_CONTRACT.md` + `contracts/NAMING_AND_OWNERSHIP.md`.

---

## 2. Identity Register

| ID | Type | Name | Slug Source | Minted By |
|----|------|------|-------------|-----------|
| TEAM-{slug} | team | {team name} | {team name → slug} | team-topologies |
| BC-{slug} | bounded-context | {context name} | {DDD-08 / component-design BC name → slug} | team-topologies |
| SVC-{slug} | service | {service name} | {service name → slug} | microservices (if active) |

**Rules:** slugs are lower-case, hyphenated, and **stable once minted** (never re-slug mid-project). Each `BC-*`/`SVC-*` maps to exactly one owning `TEAM-*` (TT-02).

---

## 3. Team → Context → Service → Contract Assignment (authoritative, machine-readable)

| TEAM | Owns BC (1..N) | Owns SVC (1..N) | Publishes Contracts | Consumes Contracts (@ pinned version — producing team) |
|------|----------------|-----------------|---------------------|--------------------------------------------------------|
| TEAM-{slug} | BC-{slug}, BC-{slug} | SVC-{slug} | {contract ref} | {contract ref}@{version} (TEAM-{slug}) |

> A team owns **1..N** contexts/services; they all stay together as modules inside that team's ONE workspace downstream. This one-team-to-many mapping is exactly what AI-DWG splits on — **team granularity, never per-service**. If one team owns everything → single workspace (no split).

---

## 4. Ownership Integrity Checks

- [ ] Every `BC-*` and `SVC-*` has exactly one owning `TEAM-*` (TT-02 — no split ownership)
- [ ] Every `TEAM-*` in `team-topology-map.md` appears here, and vice versa
- [ ] Every consumed contract resolves to a contract published by some team in this registry (TT-10)
- [ ] Every consumed contract carries a pinned version
- [ ] Slugs are unique within each type and stable since minting

---

## 5. Downstream Consumption (informational)

| Consumer | Uses this registry to… |
|----------|------------------------|
| AI-POLC | Tag epics/stories with `Owning Team` (TEAM-*) + `Bounded Context` (BC-*) so backlog slices by team |
| AI-UXD | Tag flows/journeys/wireframes with `BC-*` / `TEAM-*` so UX follows the owning team |
| AI-DWG | Partition the architecture + backlog + UX by `TEAM-*`; scope one workspace per team; build the contract registry |
| AI-GCE | Derive CODEOWNERS + cross-workspace GOV-TT rules from team ownership |

---

*Generated by AI-ADLC · AIFLC PDLC Family · © Mohammad Maheri · https://github.com/mbmd/AIFLC*
